[project]
name = "lightmap-server"
version = "0.1.0"
description = "Campus Power Monitor IoT Server"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "paho-mqtt>=2.1.0",
    "gunicorn>=23.0.0",
]
